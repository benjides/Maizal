const bfs = require('./uninformed/bfs');
const dijkstra = require('./uninformed/dijkstra');
const random = require('./uninformed/random');
const dfs = require('./uninformed/dfs');

module.exports = {
  bfs, dijkstra, random, dfs,
};
