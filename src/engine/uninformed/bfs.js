/**
 * Breadth-first search
 *
 * Expands nodes at depth "d" before going to the next level
 *
 * completness: true
 * admissability: true
 */
module.exports = {
  evaluationFn: ({ depth }) => depth,
};
